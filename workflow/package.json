{
  "name": "@platforma-open/milaboratories.functional-analysis.workflow",
  "version": "1.1.4",
  "type": "module",
  "description": "Block Workflow",
  "scripts": {
    "build": "rm -rf dist && pl-tengo check && pl-tengo build",
    "test": "vitest",
    "format": "/usr/bin/env emacs --script ./format.el"
  },
  "dependencies": {
    "@platforma-open/milaboratories.functional-analysis.software": "workspace:*"
  },
  "devDependencies": {
    "@platforma-sdk/tengo-builder": "catalog:",
    "@platforma-sdk/workflow-tengo": "catalog:",
    "@platforma-sdk/test": "catalog:",
    "@milaboratories/software-pframes-conv": "catalog:",
    "@platforma-open/milaboratories.software-small-binaries": "catalog:",
    "vitest": "catalog:"
  }
}
